name: limitless_app
description: "A new Flutter project."
publish_to: 'none'

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  
  # --- Dipendenze Progetto Originale ---
  extension_google_sign_in_as_googleapis_auth: ^2.0.7
  supabase_flutter: ^2.3.1
  provider: ^6.0.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  graphview: ^1.1.3
  intl: ^0.20.2
  cupertino_icons: ^1.0.8
  image_picker: ^1.0.4
  mime: ^2.0.0             
  google_fonts: ^6.1.0
  record: ^6.1.2           
  permission_handler: ^12.0.1 
  googleapis: ^11.0.0       
  googleapis_auth: ^1.4.0   

  # --- Nuove Dipendenze per il Digital Twin (Spostate qui!) ---
  http: ^1.2.0           # Già presente, aggiornato alla versione più recente
  path_provider: ^2.1.2  # Già presente, aggiornato alla versione più recente
  rive: ^0.13.0          #
  audioplayers: ^5.2.1   #

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: "^0.13.1"

flutter:
  uses-material-design: true
  
  # --- Registrazione Assets ---
  assets:
    - assets/images/
    - assets/rive/robot_agent.riv # Assicurati che il file si chiami esattamente così

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/logo.png" 
  adaptive_icon_background: "#ffffff" 
  adaptive_icon_foreground: "assets/images/logo.png"